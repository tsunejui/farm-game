[tools]
dotnet = "9"
just = "1.46.0"
python = "3.12"
gum = "0.17.0"
"pipx:litecli" = "1.17.1"
vendir = "0.45.0"

# GUI tools (install manually):
#   brew install --cask tiled    # Tiled map editor (https://github.com/mapeditor/tiled)
#   brew install --cask blender  # Blender for sprite pipeline (https://www.blender.org)
